# Environment variable template for local development.
# This file exists to document the SQLite connection expected by Prisma and the Next.js app.
# It interacts with prisma/schema.prisma, src/lib/prisma.ts, and local Prisma CLI commands.
DATABASE_URL="file:./dev.db"
